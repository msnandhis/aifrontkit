# Registry UI

Editable, native-first visual building blocks used by AIFrontKit components. These files own presentation, not runtime state or transport behavior.

Use semantic HTML first. Items that require complex focus management or overlays must declare and justify an audited accessibility dependency in their registry manifest.
