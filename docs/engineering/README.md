# Engineering documentation

Engineering policies currently remain in the parent `docs` directory to preserve
stable repository links while the architecture is implemented. They cover
dependency rules, testing, security, performance, release compatibility,
registry behavior, and contributor governance.

New maintainer-only implementation documents belong here. Public tutorials and
reference material belong in `content/docs`.
