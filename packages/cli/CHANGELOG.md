# aifrontkit

## 0.2.0-next.1

### Minor Changes

- 3c21447: Consolidate the public surface into four maintained packages. Move provider
  integrations behind adapter subpaths, semantic themes into React and
  deterministic test helpers into core while preserving focused tree-shakeable
  exports and documenting preview migration paths.

## 0.2.0-next.0

### Minor Changes

- b2e7c8f: Add provider-neutral frontend commands, long-running task state, task and approval primitives, schema v3 adapter mappings, read-only MCP registry discovery and signed manifest provenance.
- f4030c4: Add framework-neutral file content contracts, controlled runtime creation, typed message-part rendering, and the first-party source registry CLI with alias and provenance support.
- a45b4f9: Prepare the complete public package set for the first coordinated preview with canonical metadata, clean tarballs, executable CLI distribution, package documentation and consumer-facing release validation.
- 12b158c: Add provider-neutral schema v4 checkpoint projections, version-bound recovery commands, controlled React checkpoint primitives, LangGraph history translation and a source-installed checkpoint recovery pattern with public registry and playground distribution.
- 79bad37: Add parity-verified Tailwind source for File, Prompt Input and all six production patterns. Promote target-aware CLI installation with same-flavor dependency closure, public contract tests and visual quality fixtures.
