# aifrontkit

The source installer and read-only registry discovery CLI for AIFrontKit.

```bash
npx aifrontkit list
npx aifrontkit init
npx aifrontkit add conversation
```

The CLI records source provenance, protects edited files and can expose registry discovery through MCP. It does not execute registry code or require an AIFrontKit account.
