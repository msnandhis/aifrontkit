{
  "name": "aifrontkit",
  "version": "0.1.0",
  "description": "Install, inspect, and upgrade source-owned AIFrontKit UI",
  "type": "module",
  "license": "Apache-2.0",
  "bin": { "aifrontkit": "./dist/bin.js" },
  "files": ["dist"],
  "exports": {
    ".": "./dist/index.js",
    "./mcp": "./dist/mcp.js",
    "./provenance": "./dist/provenance.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": { "@types/node": "^24.3.0", "typescript": "^5.9.2", "vitest": "^3.2.4" }
}
