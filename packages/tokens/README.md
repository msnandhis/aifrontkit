# @aifrontkit/tokens

Semantic design tokens and generated CSS variables for AIFrontKit interfaces.

```css
@import "@aifrontkit/tokens/css";
```

Tokens are framework-neutral and can be used with package primitives or source-owned registry components.
