# @aifrontkit/ai-sdk

A structural adapter from AI SDK UI message stream parts to normalized v4 AIFrontKit events.

The adapter is optional and does not import React or execute model requests. Compatibility fixtures are pinned in the public repository.
